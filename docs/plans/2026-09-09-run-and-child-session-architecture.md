**Status:** Active

## Landed as

Committed on 2026-09-10 as eight ordered slices, then five Windows portability repairs on 2026-09-11; each SHA verified with `git cat-file -t` before recording.

| Slice | SHA | Subject |
|---|---|---|
| 1 | `8ba92f669` | feat(agent-core): session-scoped budgets, shared controller, and usage snapshots |
| 2 | `b3126872a` | feat(delegation): durable child-run records, attempts, and workspace lifecycle |
| 3 | `0bf3a309a` | feat(session): child-run lifecycle pass-throughs with status and recovery |
| 4 | `713d47f20` | feat(sdk): root aggregate budget, child concurrency, and transcript reattachment |
| 5 | `94be90227` | feat(delegate): workspace isolation input and launch evidence handle |
| 6 | `077d0ce8b` | feat(cli): agent lifecycle subcommands over the session-owned registry |
| 7 | `8e4117ba0` | feat(protocols): full child-run task surface on RPC and ACP |
| 8 | `34c53156b` | test(delegation): boundary coverage for lifecycle, budgets, and recovery |
| repair 1 | `01855109a` | fix(delegation): platform-correct claim containment and admin-entry verification |
| repair 2 | `f74eabe73` | test(delegation): tamper the worktree admin entry from the git side |
| repair 3 | `8d096b119` | fix(release): unblock Windows cleanup and preserve frozen model data |
| repair 4 | `7bfd30934` | test(release): load preload module through a portable file URL |
| repair 5 | `5473ceea9` | fix(delegation): compare ownership by exact path segments |

Full `npm test` at the stack head (`34c53156b`): `EXIT:0`, 416 coding-agent test files passed (6 skipped), 3,714 tests passed (58 skipped); agent-core 25 files, 441 passed (1 skipped). Log: `.apex-code/run-logs/full-suite-2026-09-10-final.log`.

The five repair commits above close Windows-only failures the first three-OS run exposed: backslash-blind claim containment, an `EPERM` read of the worktree `.git` pointer, an `EBUSY` scratch cleanup, a drive-letter preload specifier, and a `..`-prefixed child name bypassing ownership. Three-OS CI is green at `5473ceea9` (run `34562097691`: ubuntu, macos, windows, and the frozen-package job all success). Every SHA on this page was checked with `git cat-file -t`.

| Task | State | Evidence |
|---|---|---|
| Establish live plan and reconcile ownership | complete | `ChildRunRegistry` owns child handles and lifecycle operations |
| Complete child lifecycle and registry semantics | complete | `packages/coding-agent/src/core/delegation/runtime.ts` |
| Add public-boundary tests and run validation | complete | Added canonical overlap, shared-read, and owner-less worktree refusal tests in `packages/coding-agent/test/delegation/runtime.test.ts`. `npx vitest run packages/coding-agent/test/delegation/runtime.test.ts` passed (2 files discovered including worktree copy; 36 tests passed). `npx tsgo --noEmit` passed. |
| Wire ACP lifecycle operations | complete | ACP routes agent/list, send, wait, resume, interrupt, and close through the same `AgentSession` pass-throughs RPC calls (`AcpPromptableSession` grew the six methods; the optional `AcpHost` lifecycle callbacks and their "Child lifecycle is unavailable." stub path are removed). Payloads match RPC: list returns `[{handleId, agentType, status}]`, send/wait/interrupt/close return `null`, resume returns `{status}`; unknown child ids surface as -32000 errors raised by the same pass-throughs. `agent/resume` was added to RPC so both machine surfaces expose the same six operations (spec "Protocol and client surfaces"). From `packages/coding-agent`: `npx vitest run test/delegation test/cli test/acp test/tools/delegate.test.ts` — 17 files, 151 tests passed, including 3 rewritten `test/acp/server.test.ts` lifecycle tests that drive a real `ChildRunRegistry` through `runDelegation` (launch, unknown-ID rejection on all five id-taking operations, idempotent close) and 1 new RPC lifecycle wiring test. Repo-root `npx tsgo --noEmit` passed. Protocol payload update (2026-09-09): `agent/spawn` now exists on RPC and ACP (background only, through `AgentSession.startChildRun`, returning `{handleId}`); `agent/wait` success carries the latest settled turn as `{status, output, outcome}` (no longer an empty payload); `agent/list` entries carry `task` — `{handleId, agentType, task, status}` — on both surfaces. |
| Persist and resume child sessions | complete | Lifecycle records persist as `child_run` entries in the parent session log; `resumeChild()`/`resumeChildRun()` resume an interrupted child in the same process with its original identity, policy, and artifact ownership (runtime/protocol/CLI coverage; the former RunService tests were ported to `runtime.test.ts` when that facade was deleted). Restart reconstruction landed 2026-09-09: every `save()` now persists the durable fields (`task`, `artifactDir`, `workspace`, `depth`) plus the latest settled turn's output (older minimal records still load), and one reattachment seam in `sdk.ts` `buildChildSession` opens the recorded child session file (`SessionManager.open`) with the same derived permission/model/tool wiring — no second construction path. `ChildRunRegistry.resumeHistorical` resolves `<artifactDir>/*_<sessionId>.jsonl` (the timestamped file name is scanned, not assumed), reattaches, sends the resume prompt through `sendInput`, and tracks settlement like a live entry; `AgentSession.resumeChildRun` routes historical records through it while the live path stays interrupted-only. A historical completed run's persisted output is retrievable via `wait` without reattaching; when no settlement was persisted the error states the output cannot be recovered. Actionable refusals: never-file-backed (in-memory) children, missing artifact directory, missing child session file, closed runs, released worktrees; depth and capability ceiling are re-admitted at reattach through the shared projection (`resolveAdmittedDefinition`). E2E (`test/delegation/end-to-end.test.ts`): a completed background child survives parent dispose + parent-session reopen — the historical record is listed, its persisted output retrievable, and `resumeChildRun` reattaches the SAME child session file (header id unchanged, history grows by the resumed turn); legacy minimal records and missing artifact dirs refuse with actionable errors. Superseded 2026-09-09 by the "Workspace states and explicit recovery" row: retained worktrees now classify (`retained-dirty`/`retained-failed`/`released`/`missing`) and recover explicitly via `recoverChildWorkspace` before resume; output still requires a persisted settlement. From `packages/coding-agent`: `npx vitest run test/delegation test/cli` — 14 files, 128 tests passed (3 new in `end-to-end.test.ts`); plus `test/tools/delegate.test.ts` + `test/acp` — 3 files, 35 tests passed. Repo-root `npx tsgo --noEmit` passed; `biome check` on the touched files passed. |
| Reuse budgets, verification, evidence, and artifacts | complete | Budget and verification reuse landed 2026-09-09, with the budget contract superseded the same day by the root budget contract (spec "Shared budgets"). Budget (root contract, current): policy inheritance (`CreateAgentSessionOptions.runBudget` overrides the Agent policy; `settingsManager.getRunBudget()` stays the default; `buildChildSession` forwards the parent's already-resolved policy) — per-run only, never a shared ceiling; the aggregate exists only through explicit `CreateAgentSessionOptions.aggregateBudget`, which creates ONE root aggregate controller (`createRunBudgetController`) per root tree at root session construction; the root session's own Agent consumes it via the existing `sharedBudgetController` seam (composite own + shared; root `budgetScope` stays "prompt", so per-prompt local semantics are unchanged and the aggregate survives prompts); `buildChildSession` forwards THE SAME aggregate instance to every child regardless of depth, so grandchildren consume the root ledger, not a fresh per-parent ledger (the earlier family-ledger-from-`runBudget` behavior — one per-session ledger making each session the family parent of its direct children — is deleted as the bug this contract replaces); with no `aggregateBudget` there is NO shared controller anywhere: children keep local-only budgets; children are still built with `budgetScope: "session"` so one controller per child Agent continues across sendInput/follow-up/resume turns; composition stays `createCompositeBudgetController` (gates allow only if both allow, accepted attempts record to both, the stock tool-call boundary is preserved, `exhaustedLimit` names the child's own limit first). Usage: `AgentSession.aggregateBudgetUsage()` reports `{providerRequests, toolCalls, maintenanceRequests, startedAtMs}` for the whole tree from the one aggregate controller. Concurrency: `CreateAgentSessionOptions.maxConcurrentChildren` bounds simultaneously-active child runs at the delegation runtime's admission path — over the limit admission throws an actionable error naming the limit BEFORE any child is built; a slot is released on terminal settlement (completed/failed/interrupted), close, registry disposal, or launch failure; `resumeHistorical` counts against the limit while running and releases on terminal. Verification follow-up semantics settled test-first: a follow-up queued onto a live child run settles inside the run that owns the settlement gate, and with a failing post-turn policy the settled outcome is failed — the parent-side gate skip on the running path is safe because the child boundary owns it (regression test kept). Verification: the SDK child handle's `run()` and settled follow-up/resume turn path require `completionStatus() === "verified"` when the child has configured policies — "explicit" runs `requestVerification()` once, "post-turn" reads the status the session already produced at its turn boundary, any other status fails the turn with the status named, and the failure flows through the existing `latestResult`/settlement path so the registry records `failed`; `AgentSession.verificationPolicyIds()` is the read through the canonical tracker (no second verifier). Evidence: from `packages/coding-agent`, `npx vitest run test/delegation test/cli test/tools/delegate.test.ts` — 15 files, 130 tests passed (5 new in `test/delegation/end-to-end.test.ts`); repo-root `npx tsgo --noEmit` passed. Aggregate-budget and follow-up-verification evidence (2026-09-09): from `packages/agent`, `npx vitest run test/run-budget.test.ts test/agent.test.ts test/agent-loop.test.ts` — 3 files, 65 tests passed (10 new in `test/run-budget.test.ts`: budget scope + composite controller, fake `now()` for wall time); from `packages/coding-agent`, `npx vitest run test/delegation test/tools/delegate.test.ts` — 5 files, 84 tests passed (3 new in `test/delegation/end-to-end.test.ts`: child budget continuity across sendInput with a third turn refused at the gate, family ledger exhausted across two children with the mid-batch refusal named `tool-calls` in the child transcript, and the failing-post-turn queued follow-up); repo-root `npx tsgo --noEmit` and `npm run check:docs` passed. Root budget contract evidence (2026-09-09, supersedes the family-ledger evidence above; the "family ledger exhausted across two children" test became "exhausts the aggregate budget across two children", now configured via `aggregateBudget`): tests written first and watched to fail for the right reason (no aggregate: grandchild tree sent 5 requests instead of 4; local-only test sent 1 instead of 2 under the old ceiling; concurrency admissions were not refused). From `packages/agent`, `npx vitest run test/run-budget.test.ts test/agent.test.ts test/agent-loop.test.ts` — 3 files, 65 tests passed (no new agent primitives needed). From `packages/coding-agent`, `npx vitest run test/delegation test/tools/delegate.test.ts test/cli test/acp test/agent-session-concurrent.test.ts test/rpc-prompt-response-semantics.test.ts` — 18 files, 188 tests passed, including 6 new/renamed in `test/delegation/end-to-end.test.ts` ("parent runs consume the configured aggregate budget", "grandchildren share the root aggregate ledger, not a fresh per-parent ledger", "without aggregateBudget children keep local-only budgets and do not share a ceiling", "exhausts the aggregate budget across two children") and 3 new in `test/delegation/runtime.test.ts` ("admission refuses a child over maxConcurrentChildren and releases capacity on terminal settlement", "admission releases capacity on delegation failure and on close", "historical resume counts against maxConcurrentChildren and releases on terminal settlement"). Repo-root `npx tsgo --noEmit` (exit 0) and `npm run check:docs` ("Documentation lifecycle validation passed.") passed; `biome check` clean on sdk.ts, delegation/runtime.ts, and both touched test files. The row's last remainder (policy/sandbox/artifact linkage) landed 2026-09-09 in the "Policy, sandbox, artifact, and evidence linkage" row below. Evidence linkage settled 2026-09-09: per-child session linkage is the evidence linkage model — each delegated child's session file lives under the parent session dir's `delegations/<handleId>/` with `parentSession` set in its header and the handle in the file name (`test/delegation/end-to-end.test.ts` asserts the file, the header id == handle, and parentSession == the parent's session id), and `child_run` records persist in the parent session log. The delegate contract's `capture()` runs in `afterToolCall` with the completed result, so background launches additionally carry `handle` on the existing `WorkflowEvidenceRecord.handle` field, tying an evidence record to that child session file — no canonical contract or sink shape change (`test/tools/delegate.test.ts`). |
| Add path ownership and worktree isolation | complete | Path claims, the owner-less worktree refusal, and the production owner are implemented and wired. `ChildWorkspaceRequest.root?` carries the owner's resolved isolation root (`delegation/runtime.ts`, which now consults a `ChildWorkspaceOwner` only for `isolation: "worktree"`, so shared-read delegations construct no owner and invoke no git); `GitWorktreeWorkspaceOwner` (`core/workspace/git-worktree-owner.ts`, all git through the workspace subsystem's shared `runGit` seam) creates the worktree at `<workspaceRoot>/.apex-code/worktrees/<sessionId>` on branch `apex-child-<short-sessionId>`, keeps that directory out of the parent checkout's status via the repo-local `.git/info/exclude` (never touching tracked files such as `.gitignore`), removes it non-force first and **never auto-forces**: when the plain remove refuses, the tree is inspected (`git status --porcelain`) and a dirty tree is *kept*, reported as a `WorkspaceReleaseOutcome` of `{removed:false, kept:"dirty", dir}` (a clean tree that still cannot be removed reports `kept:"failed"` with the git error); only an explicit `release(id, {force:true})` force-removes. The `ChildWorkspaceOwner.release` signature returns that outcome type, and the registry stores the last outcome per session id (`workspaceReleaseOutcome(id)`), warning loudly on kept outcomes (dirty: dir + "kept; uncommitted child work preserved" + the manual `git worktree remove --force <dir>` hint; failed: the error), tolerates already-removed, and throws the refusal outside a git repository so the delegation fails with the owner's error. `sdk.ts` constructs the owner lazily (getter: first worktree-isolated delegation) and `buildChildSession` runs the child in `preparedWorkspace.root ?? cwd`; the built runtime is exposed as `CreateAgentSessionResult.delegationRuntime`. E2E against the real owner and real child sessions through `runDelegation` (`test/delegation/end-to-end.test.ts`): a worktree-isolated child's write lands in the worktree (the child session header's `cwd` records it) and not in the parent checkout, with `git status --porcelain -uall` clean; release removes the worktree (dir gone, `git worktree list` clean, repeat tolerated); a shared-read child's cwd is unchanged and no worktree is created; a non-repo refusal builds no child. From `packages/coding-agent`: `npx vitest run test/delegation test/cli test/acp test/tools/delegate.test.ts` — 17 files, 155 tests passed. Repo-root `npx tsgo --noEmit` passed; `biome check` on the changed files passed. Repo `.gitignore` ignores `.apex-code/worktrees/` by path. |
| Remove duplicate legacy paths and update durable docs | complete | Legacy `WeakMap` registry and retrieval-specific handle semantics are gone (registry owns handles; `delegate` is a thin adapter). The `RunService` facade was subsequently **deleted, not populated** (populate-or-delete decision, 2026-09-09): it was test-only with unpopulated `RunRecord` fields, so `ChildRunRegistry` is the sole lifecycle owner and `AgentSession` is the only external surface (see the spec's ownership section and deletion inventory; its two uncovered behaviors were ported to `test/delegation/runtime.test.ts`). Durable docs updated: spec Status Active with the ownership resolution recorded, roadmap follow-up row added (`npm run check:docs` passes). |
| Run final full validation | complete | Repo root: `npx tsgo --noEmit` passed; `npm run check:docs` passed; full `npm test` completed with `EXIT:0` — 417 test files passed, 6 skipped; 3,669 tests passed, 58 skipped (log: `.apex-code/run-logs/full-suite-2026-09-09.log`). |
| Audit completion against the spec | complete | Code review found the implementation remains partial |
| Unified RunService and RunRecord | complete | Decision 2026-09-09: **deleted, not populated** — `run-service.ts` was a test-only facade with unpopulated `RunRecord` fields; the session-owned `ChildRunRegistry` is the sole lifecycle owner (spec ownership section records this). The durable parts live in the registry: retrieval follows the latest settled turn per the spec's `wait` requirement (`ChildSessionHandle.latestResult()` reports each turn's `{output, outcome}`; `ChildRunRegistry.retrieve()`/`wait()` serve the per-handle latest result — completed turns return text, failed turns rethrow, interrupted first turns resolve as `interrupted` instead of re-raising; late settlement never overwrites a closed run's persisted status). Its two behaviors not covered at the registry boundary were ported to `test/delegation/runtime.test.ts` (failure reflected without explicit wait; input accepted immediately after awaited startup). Restart reconstruction for resume lives in historical reattachment (see "Persist and resume child sessions"). Evidence: `npx vitest run test/delegation/runtime.test.ts` — 31 tests passed after the deletion and ports. |
| Real resume behavior | complete | `resumeChild()` and its resume tests pass; restart reconstruction landed via historical reattachment (see "Persist and resume child sessions"): a completed background child survives parent dispose + parent-session reopen, and `resumeChildRun` reattaches its original child session file (same header id, history grows by the resumed turn), while legacy minimal records and missing artifact dirs refuse with actionable errors. Superseded 2026-09-09 by the "Workspace states and explicit recovery" row: worktree children classify and recover explicitly before resume. Runs whose settlement was never persisted still cannot serve output without resuming. Evidence and validation live in the "Persist and resume child sessions" and "Workspace states and explicit recovery" rows. |
| Budget, verification, and evidence integration | complete | The "no injection seam" blocker is resolved for budget and verification (see "Reuse budgets, verification, evidence, and artifacts" above). Evidence linkage is settled: per-child session linkage (parentSession header + `delegations/<handleId>/` artifact dir + `child_run` persistence records) is the model, with `handle` carried on the launch's workflow evidence record from the completed result — no contract or sink shape change (see the "Reuse budgets..." row for the tests). The RunRecord artifact/policy linkage remainder landed 2026-09-09 (see the "Policy, sandbox, artifact, and evidence linkage" row). The root budget contract has landed (see "Reuse budgets, verification, evidence, and artifacts"): the aggregate is explicit via `aggregateBudget`, the parent's own runs consume it, one aggregate serves the whole root tree, and `maxConcurrentChildren` caps admissions. The one deliberately open decision, recorded in the spec's risk note, is whether an aggregate budget default should turn on for every session (awaits replay measurements); it is a settings-policy decision, not missing work. |
| CLI lifecycle operations | complete | `apex-code agent list\|wait\|send\|resume\|interrupt\|close <childId> [input]` — classified session-kind (sandboxed) in `packages/coding-agent/src/cli/args.ts`, executed after normal session construction via the session-owned registry in `packages/coding-agent/src/cli/agent-lifecycle.ts`; `AgentSession.resumeChildRun()` added. `npx vitest run test/delegation test/cli test/args.test.ts test/session-id-readonly.test.ts test/extensions-discovery.test.ts test/credential-print.test.ts` from `packages/coding-agent`: 18 files, 228 tests passed. `npx tsgo --noEmit` passed. Cross-process lifecycle against a persisted session is closed by historical reattachment (see "Real resume behavior" and "Persist and resume child sessions"). |
| Actual worktree creation and cleanup | complete | `GitWorktreeWorkspaceOwner` is wired (evidence in "Add path ownership and worktree isolation"). Both closure gaps from the open boundary are now closed: (1) lifecycle release — `ChildRunRegistry` tracks each worktree session id (`trackWorkspace`, set by `runDelegation` when it consults the owner), holds the owner (`setWorkspaceOwner`), and releases through it exactly once per session id (`releaseWorkspaceOnce`/`releaseWorkspaceNow`) on `close()`, `dispose()`, and the delegation failure path; release is best effort — a missing owner is tolerated, an owner failure never escapes close/dispose, and every release outcome is stored per session id (`workspaceReleaseOutcome(id)`) with a loud `console.warn` for kept trees (dirty: "uncommitted child work preserved" + the manual `--force` removal hint; failed: the error); the existing claim cleanup is unchanged. E2E: `test/delegation/end-to-end.test.ts` "disposing the parent session releases a clean worktree child's worktree and keeps a dirty one" asserts a dirty tree survives dispose with its uncommitted file intact and the warning emitted, while the safe-release tests cover dirty-keep, explicit `force:true` removal, clean removal, and missing-dir tolerance. (2) model-facing workspace input — the `delegate` tool's launch shape now carries `workspace?: { isolation: "shared-read" \| "worktree"; ownedPaths?: string[] }` in both the launch variant and the advertised `properties` (retrieval shape untouched; tool contract, `ruleForCall`, and evidence unchanged), passed through to `runDelegation`. Tests (`test/tools/delegate.test.ts`): a worktree request reaches `runDelegation` (built child sees the prepared root), the no-workspace default stays shared-read with no owner construction, and `ownedPaths` round-trip to the owner's `prepare()` and the built child. From `packages/coding-agent`: `npx vitest run test/delegation test/cli test/acp test/tools/delegate.test.ts` — 17 files, 159 tests passed. Repo-root `npx tsgo --noEmit` passed; `biome check --write` then `biome check` on the four touched files passed. |
| Final full-suite verification | complete | Full `npm test` completed `EXIT:0`: 417 test files passed (6 skipped), 3,669 tests passed (58 skipped). Log retained at `.apex-code/run-logs/full-suite-2026-09-09.log`. |
| Attempt records, idempotent spawn, timeouts, cancellation reasons, pollable status | complete | Landed 2026-09-09 (test-first; every new test watched failing before implementation). Data shape (one owner: `ChildRunRecord` in `packages/coding-agent/src/core/delegation/runtime.ts`): `ChildRunAttempt {id, startedAt, endedAt?, outcome?: completed\|failed\|interrupted\|cancelled, error?, usage?}`; the record gains `attempts`, `activeAttemptId`, `idempotencyKey`, `deadlineMs`, `cancelled: {reason, at}`; `restore()` synthesizes one attempt from `status`/`updatedAt` for legacy records so older logs load unchanged. Semantics: launch = attempt 1; follow-ups/steering stay in the active attempt; `resumeHistorical` and `AgentSession.resumeChildRun` (via the new `registry.beginResumeAttempt`) close the prior attempt with its terminal outcome and open a new one; interrupt with a reason persists `cancelled` and marks the attempt `cancelled`, plain interrupt settles `interrupted`; attempt `usage` snapshots the child's own controller via the new optional `ChildSessionHandle.usage()` (backed by `Agent.budgetUsage()` in `packages/agent`, `undefined` before the first run creates a controller — no second budget classification). Behaviors: (1) idempotent spawn — `startChildRun` and `runDelegation` accept `idempotencyKey`; the registry keeps a key→handle map (rebuilt by `restore()` so a restarted parent dedupes) and a duplicate returns the existing handle before admission, consuming no second slot; (2) timeout — spawn accepts `timeoutMs`: the child's OWN budget is built with `maxWallTimeMs=timeoutMs` (sdk `buildChildSession`), the record stores `deadlineMs=Date.now()+timeoutMs`, and `status()`/`list()` observe it lazily, interrupting a running child past its deadline (no reason, so not user-cancelled); a launch refused at that gate settles failed naming `wall-time` in the existing `exhaustedLimit` vocabulary (sdk `run()` reads the child's `agent_end` stopReason, which is the only signal for a refusal that produces no assistant message); (3) `interruptChildRun(id, reason?)` through registry, RPC, and ACP; (4) pollable `ChildRunRegistry.status()` / `AgentSession.childRunStatus()` / `agent/status` on RPC and ACP return `{handleId, agentType, task, status, attempt, attempts, lastResult?, cancelled?, deadlineMs?, usage?, workspace?}` built from record/entry only — never awaiting a turn; `wait` unchanged; (5) `list` gains `attemptCount` only. Tests named exactly as specified: `test/delegation/runtime.test.ts` ("spawn with the same idempotency key returns the original handle without a second child", "legacy records without attempts load with one synthesized attempt", "resume opens a new attempt and closes the prior one", "interrupt with a reason records cancelled on the record and attempt", "status reports a timed-out running child and interrupts it"); `test/delegation/end-to-end.test.ts` ("timeoutMs bounds a child run at the wall-time gate" — timeoutMs 0 drives the wall-time gate budget-only, no sleeps: zero provider requests fire and the rejection names `wall-time`); `test/rpc-prompt-response-semantics.test.ts` + `test/acp/server.test.ts` (spawn `{handleId, created}` with duplicate key `created:false` on the same id, `agent/status` returning the task shape without blocking, interrupt carrying a reason into `cancelled`). Evidence: from `packages/coding-agent`, `npx vitest run test/delegation test/tools/delegate.test.ts test/cli test/acp test/agent-session-concurrent.test.ts test/rpc-prompt-response-semantics.test.ts` — 18 files, 200 tests passed (12 new). From `packages/agent`, `npx vitest run test/run-budget.test.ts` — 1 file, 11 tests passed (1 new: `Agent.budgetUsage()` counters across session-scoped prompts; the agent package's `dist` was rebuilt since coding-agent tests resolve `apex-code-agent-core` from it). Repo-root `npx tsgo --noEmit` (exit 0) and `npm run check:docs` ("Documentation lifecycle validation passed.") passed; `npx biome check` on all 13 touched files clean. Not done here (later phases): workspace orphan recovery, artifact/evidence expansion, settings defaults. |
| Workspace states and explicit recovery | complete | Landed 2026-09-09 (test-first; all new tests were written against the specified behaviors before implementation, and the three runtime-semantics tests were each watched failing for the right reason before the implementation existed — reconciliation absent (status stayed "running"), `workspaceState` absent, `recoverWorkspace is not a function`; the protocol and e2e tests were first executed after the wiring landed and failed only on test-harness issues — a missing `mkdtempSync` import, and child_run records unflushed to the parent session file until a parent turn, the same buffering the reattachment tests already document — then passed once correct). Persisted state (one owner: `ChildRunRecord.workspaceState?: "active" \| "released" \| "retained-dirty" \| "retained-failed" \| "missing"` in `packages/coding-agent/src/core/delegation/runtime.ts`; absent = active so legacy records load unchanged; written only for worktree-isolated records): release outcomes classify and persist immediately even though release is fire-and-forget (`ChildRunRegistry.recordWorkspaceState`, called from `releaseWorkspaceOnce`: removed → "released", kept:"dirty" → "retained-dirty", kept:"failed" → "retained-failed"); a missing/nonexistent recorded root observed at resume classifies "missing" (persisted on the record) and refuses. Stale-running reconciliation: `restore()` rewrites a persisted "running" record's in-memory status to "interrupted" and closes its active attempt with outcome "interrupted"; the historical JSONL is never rewritten by restore — the corrected status persists with the record's next save; restart semantics documented in the spec: resume continues from the last persisted transcript boundary, not exactly-once. Explicit recovery: `AgentSession.recoverChildWorkspace(id)` → `ChildRunRegistry.recoverWorkspace(id)` — only for worktree-isolated records whose recorded root exists; verification is read-only through the workspace owner's new optional `verify()` (`GitWorktreeWorkspaceOwner.verify`: (a) admin entry — `<dir>/.git` pointer file resolving into the CURRENT repository's `.git/worktrees` (via `rev-parse --absolute-git-dir`), (b) checked-out branch `apex-child-<short-sessionId>` (`rev-parse --abbrev-ref HEAD`), (c) the owner's `<root>/.apex-code/worktrees/<sessionId>` layout; no create, checkout, reset, or force anywhere); on success `workspaceState` becomes "active" (persisted) and the call returns `{workspaceState: "active", dirty}`; every failed check throws naming the failed check with the workspace left unverified. Plain resume of retained-dirty/retained-failed refuses naming the persisted state and pointing at `recoverChildWorkspace` (`rejectUnresumableWorktree`, which runs before the closed-status refusal so the actionable gate wins). Protocol: `agent/recover` on RPC (command `{childId}`, `AgentRecoverResult {workspaceState, dirty}` response) and ACP (`recoverChildWorkspace` on `AcpPromptableSession`, `agent/recover` dispatch case, refusals as -32000); `agent/status` payloads gain `workspaceState` for worktree-isolated runs (`statusFromEntry` prefers the persisted record's state, `statusFromRecord` defaults absent → "active"); `agent/list` entries gain nothing. Tests named exactly as specified: `test/delegation/runtime.test.ts` ("restored running records become interrupted with an interrupted attempt" — also asserts restore persists nothing, "release outcomes persist workspaceState on the record" — stub owner returning `{removed:true}` and `{removed:false, kept:"dirty"}` through real close-path releases, "recoverWorkspace verifies the admin entry and branch before reactivating" — real temp git repo + `GitWorktreeWorkspaceOwner`: detached-HEAD tamper forces the branch-check refusal with nothing persisted, a tampered admin entry forces the admin-entry refusal, then the clean recover returns `{workspaceState:"active", dirty:false}` and an uncommitted file reports `dirty:true` with the file intact); `test/delegation/end-to-end.test.ts` ("a retained-dirty worktree child classifies, refuses resume with guidance, and recovers explicitly before resuming in the SAME worktree" — real git: writer child leaves an uncommitted file in its worktree, a parent turn flushes the child_run records, parent dispose keeps the tree and classifies it, parent reopen shows `workspaceState: "retained-dirty"`, `resumeChildRun` refuses naming the state and `recoverChildWorkspace`, `recoverChildWorkspace` returns `dirty:true`, resume then completes inside the SAME worktree with the uncommitted file intact and the child header cwd unchanged); `test/rpc-prompt-response-semantics.test.ts` + `test/acp/server.test.ts` ("agent/recover verifies and reactivates a worktree child, and refuses a child without one" on both surfaces — happy path `{workspaceState:"active", dirty}` through a stub owner, refusal for a shared-read child, `agent/status` carrying `workspaceState` for a worktree child). One existing phase-2 assertion updated for behavior this phase intentionally changed: the legacy-record test's restored "running" fixture now asserts the reconciled outcome (status "interrupted", attempt outcome "interrupted") instead of the stale pre-reconciliation report. Evidence: from `packages/coding-agent`, `npx vitest run test/delegation test/tools/delegate.test.ts test/cli test/acp test/agent-session-concurrent.test.ts test/rpc-prompt-response-semantics.test.ts` — 18 files, 206 tests passed (6 new). Repo-root `npx tsgo --noEmit` (exit 0) and `npm run check:docs` ("Documentation lifecycle validation passed.") passed; `npx biome check` on all 10 touched files clean. Not done here (later phases): automatic workspace recreation stays disabled, artifact/evidence expansion, usage/cost accounting beyond what exists, settings defaults. |
| Policy, sandbox, artifact, and evidence linkage | complete | Landed 2026-09-09 (test-first: the two specified end-to-end tests, the RPC and ACP payload tests, and the legacy-record test were each watched failing before implementation — `policy` undefined and the linkage fields absent from payloads; the delegate test adds the required assertion of the already-landed evidence-handle linkage, so it passed as designed). Data shape (one owner: `ChildRunRecord` in `packages/coding-agent/src/core/delegation/runtime.ts`): `ChildRunPolicySnapshot {tools, capabilities, sandbox, maxDelegationDepth, model?, budgetScope: "prompt"\|"session", aggregateBudget}` plus `sandboxEnforced?: boolean`; `parentSessionId` is now actually populated via the new optional `DelegationRuntimeOptions.getParentSessionId` (wired by the sdk to the session manager). Population point: the sdk's `buildChildSession` — `BuildChildSessionRequest` gains `capabilities` from the shared admission projection (`resolveAdmittedDefinition` now returns the admitted set alongside the definition; no second classification, ADR 0010), and the returned handle reports `policy` (tools from the request's tool allowlist; capabilities sorted; `sandbox: sandboxContract`; `maxDelegationDepth` from the runtime's single settings read; `model: childModel.id`; `budgetScope: "session"`; `aggregateBudget: sharedBudgetController !== undefined`) and `sandboxEnforced` (the parent session's OS-containment supervisor marker check; always true under the "required" contract). `runDelegation` relays the handle's report onto the entry at launch, `resumeHistorical` carries the persisted values across reattach (the fresh handle's report wins), `save()` persists `policy`/`sandboxEnforced`/`parentSessionId` with every save, and legacy records without them load unchanged with status omitting the fields. Session-file resolution moved onto one non-throwing helper (`resolveSessionFile`, newest match wins), shared by `resolveHistoricalSession` and both status builders, so status resolves `<artifactDir>/*_<sessionId>.jsonl` lazily without throwing. Protocol: `agent/wait` (`AgentWaitResult` via `AgentSession.waitChildRunResult`) and `agent/status` (`ChildRunStatus`) payloads gain additive `artifactDir?`, `sessionFile?`, `parentSessionId?`, `policy?`, `sandboxEnforced?` on RPC and ACP (the ACP fake session mirrors the same pass-through); existing `wait` fields untouched, `agent/list` gains nothing. Evidence: the delegate launch's workflow evidence record carries `handle` from the completed result, now asserted equal to the child_run record's `handleId` for the same launch — no canonical contract or sink change. Tests named exactly as specified: `test/delegation/end-to-end.test.ts` ("child records persist the derived policy snapshot and sandbox flag" — restricted read-only child; asserts tools/capabilities/sandbox/maxDelegationDepth/model/budgetScope/aggregateBudget against reality, `sandboxEnforced` false without the supervisor marker and true with it, the persisted record read back from the parent session JSONL, and the reopened parent's restored snapshot; "wait and status surface artifact, session file, parent linkage, policy, and sandbox" — file-backed parent; both payloads carry `artifactDir`, a `sessionFile` that exists on disk ending `_<handleId>.jsonl`, `parentSessionId` equal to the parent's session id, `policy`, and `sandboxEnforced`; plus "legacy records without policy and sandbox fields load and status omits them"); `test/rpc-prompt-response-semantics.test.ts` and `test/acp/server.test.ts` ("agent/wait and agent/status carry/payloads carry ... linkage" — stub-host fixtures with a transcript stub so `sessionFile` resolves to a real file); `test/tools/delegate.test.ts` ("background launch evidence handle matches the child_run record handleId"). Evidence: from `packages/coding-agent`, `npx vitest run test/delegation test/tools/delegate.test.ts test/cli test/acp test/agent-session-concurrent.test.ts test/rpc-prompt-response-semantics.test.ts` — 18 files, 212 tests passed (6 new; baseline before the change was 206). Repo-root `npx tsgo --noEmit` (exit 0) and `npm run check:docs` ("Documentation lifecycle validation passed.") passed; `npx biome check` on all 9 touched files clean. Not done here (next phase): token/cost accounting, settings defaults. |
| Token and cost usage accounting | complete | Landed 2026-09-09 (test-first; all six specified tests were each watched failing before implementation — attempt `tokensAtEnd` undefined, `registry.usageTotals`/`session.childRunUsageTotals` "not a function", and the RPC/ACP status payloads missing `tokens`/`cost`). Data shape (one owner: `ChildRunUsageTotals {inputTokens, outputTokens, cacheReadTokens, cacheWriteTokens, totalTokens, cost: {input, output, cacheRead, cacheWrite, total}, asOf, entriesCounted}` in `packages/coding-agent/src/core/delegation/runtime.ts`) plus `ChildRunAttempt.tokensAtEnd?: ChildRunUsageTotals` and additive `ChildRunStatus.tokens?`/`cost?`. One owner, one source: the registry rolls usage up from the child's OWN transcript through the existing session-file reading seam (`SessionManager.loadEntriesFromFile` over the `resolveSessionFile` result; no second transcript cache — only the last-computed totals are cached per handle, keyed by file size+mtime, or entry count + last id for the in-memory seam). Summing rule: provider-reported numbers summed as-is — cost is provider-reported so cache reads are never re-priced, re-counted, or double-counted; entries without usage count as zero; assistant messages carry per-turn usage, and compaction/branch-summary entries carry their summarization call's usage (additional real spend, included). Reachability: file-backed runs read the transcript file; in-memory children fall back to the new optional `ChildSessionHandle.sessionEntries()` seam (backed by the sdk handle's own `childSessionManager.getEntries()`, read-only); when neither is reachable `usageTotals` returns `undefined` and status omits `tokens`/`cost` — never zero-filled, never a throw (legacy records without any transcript included; unknown ids still error like `status`). Attempt rollup: `stampAttemptSettlement` stamps the active attempt's `tokensAtEnd` at settlement, and both resume-close paths (`beginResumeAttempt` live, `resumeHistorical` historical) stamp it when the pre-restart settlement never produced one; these are cumulative-at-end snapshots of the whole transcript, documented as never per-attempt deltas (turns settled after the last persisted boundary are re-run on resume, so the transcript cannot attribute usage to one epoch). Surfaces: `ChildRunRegistry.usageTotals(id)` and `AgentSession.childRunUsageTotals(id)` pass-through; `agent/status` carries flattened `tokens` + `cost` beside the unchanged request-count `usage`, identically on RPC (`AgentStatusResult = ChildRunStatus`) and ACP (`childRunStatus` pass-through) — RPC and ACP gained zero protocol-specific code; `agent/wait` and `agent/list` unchanged. Settings defaults untouched. Tests named exactly as specified: `test/delegation/end-to-end.test.ts` ("child usage totals roll up tokens and cost from the child transcript" — file-backed parent, the one construction seam stubbed to write a real two-turn child transcript with known usage, totals asserted against the exact sum with status carrying the flattened `tokens`/`cost`; "usage totals survive restart from the persisted transcript" — parent turn flushes the child_run record, dispose, parent reopened on the same session file, the historical record's `usageTotals` computed from the file matches); `test/delegation/runtime.test.ts` ("attempt snapshots carry cumulative token totals at settlement" — stub handle with a file-backed transcript: settlement stamps `tokensAtEnd`, a follow-up in the SAME attempt re-stamps a larger cumulative snapshot (160/18/9/9/196 tokens, 3 entries), status and `usageTotals` carry the same rollup; "in-memory child without reachable transcript omits tokens in status" — no artifact dir and no seam: `tokens`/`cost`/`usageTotals` all undefined without throwing); `test/rpc-prompt-response-semantics.test.ts` and `test/acp/server.test.ts` ("status payload carries tokens/cost through fixtures" — the fixture now writes a VALID one-turn transcript with known usage via a new `usageTranscript` linkage option, and the over-the-wire status payload carries the exact rollup). Evidence: from `packages/coding-agent`, `npx vitest run test/delegation test/tools/delegate.test.ts test/cli test/acp test/agent-session-concurrent.test.ts test/rpc-prompt-response-semantics.test.ts` — 18 files, 218 tests passed (6 new; baseline before the change was 212), re-run clean after the final import-order fix. Repo-root `npx tsgo --noEmit` (exit 0) and `npm run check:docs` ("Documentation lifecycle validation passed.") passed; `npx biome check` on all 7 touched files clean. Not done here: settings defaults (explicitly out of scope). |
