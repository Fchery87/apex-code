# Plan: Production graduation and release integrity

**Status:** Active — specification and sole-maintainer operating decision accepted; implementation not started.

## Purpose

Execute [`docs/specs/2026-08-16-production-graduation-and-release-integrity.md`](../specs/2026-08-16-production-graduation-and-release-integrity.md)
test-first. Technical security-boundary decisions land before implementation; artifact and
release gates consume those decisions rather than creating parallel policy. Human monitoring,
GitHub branch protection, and maintainer availability are documented commitments and are not
pretended to be test-verifiable.

## Task table

| ID | Task | Depends on | State | Commit / evidence |
| --- | --- | --- | --- | --- |
| 12.1 | Settle the credential/state ownership and sandbox handoff contract in an ADR. Define host-owned versus ephemeral state, allowed environment, credential projection, cleanup, and session persistence without widening mounts. | — | complete | `eaddbef8e`; ADR 0015; verified 2026-08-16 |
| 12.2 | Settle supervisor trust-policy precedence in an ADR. Ensure untrusted project settings cannot influence mounts, network policy, command/args, credentials, or executable resolution before trust; preserve ADR 0005 fail-closed behavior. | — | complete | `eaddbef8e`; ADR 0016; verified 2026-08-16 |
| 12.3 | Implement and test the credential/state handoff at the public CLI boundary. Prove sanitized environment construction, no forbidden host-path access, no secret/session leakage, and normal/crash cleanup on Linux/macOS; preserve Windows portability behavior. | 12.1, 12.2 | complete | end-to-end credential-turn test (`test/sandbox/credential-handoff.test.ts`) drives the real CLI entry point through a Linux-enforced sandbox with a host-owned `auth.json`, proving the child reads the exact projected credential and a write attempt is rejected without crashing the turn, while the host's copy is unaffected; regression-proven by reverting the `authPath` wiring in `cli.ts` and confirming the test fails. Fixed a real crash-cleanup gap in `linux-backend.ts`: the read-only credential file descriptors opened via `openSync` were closed only on the successful-launch path, leaking on a rejected `waitForExit` (e.g. `bwrap` vanishing mid-launch); wrapped in `try/finally`, covered by `test/sandbox/linux-backend.test.ts`'s new "Linux sandbox crash cleanup" describe block (asserts via `/proc/self/fd` that no descriptor targeting the credential file survives), regression-proven the same way. Added the symmetric macOS credential read-only/write-rejection backend test (gated on macOS CI; this dev environment is Linux-only). `npx tsgo --noEmit` clean on all changed files; full `test/sandbox` suite (14 files / 56 passed / 5 skipped) green. |
| 12.4 | Implement and test trust-first supervisor policy resolution. Add malicious project fixtures and explicit trusted-policy coverage; prove project settings cannot widen pre-child security authority. | 12.2 | complete | `eaddbef8e`; focused launch suite passed; verified 2026-08-16 |
| 12.5 | Settle the downloaded-resource integrity model in an ADR, then pin and verify executable tool artifacts. Use bounded downloads, archive/path validation, digest verification, quarantine, atomic promotion, and tamper rejection. | 12.2 | complete | ADR 0017; `src/utils/tools-manager.ts` rewritten to install `fd`/`rg` only from a reviewed pin table (exact version, asset name, SHA-256, byte size per platform/architecture) instead of resolving GitHub's mutable "latest" release. Every pinned digest and size was independently established: downloaded locally, hashed with `sha256sum`, and cross-checked against ripgrep's own published `.sha256` sidecars and GitHub's per-asset `digest` API field for both projects — all matched exactly. `downloadFile()` now enforces a bounded ceiling (4x reviewed size) against both the declared `content-length` and the actually received byte count, aborting and deleting the partial file if exceeded. `installManagedToolArchive()` verifies the archive's digest before any extraction, extracts into a same-filesystem quarantine directory, checks the located binary resolves inside that quarantine (path-containment/zip-slip defense), then atomically renames it into the live tools directory; the quarantine directory is always removed after. A platform/architecture with no reviewed pin (e.g. win32/arm64 `ripgrep`, which upstream does not publish) fails closed with a clear error instead of building a URL that 404s. `test/tools-manager-integrity.test.ts` (5 tests): pinned-metadata shape, digest-mismatch rejection (no file promoted), oversized-archive rejection, unpinned-combination rejection, and a real tar.gz install proving atomic promotion, correct executable mode, and quarantine cleanup. Also live-network-verified outside the test suite: `ensureTool("rg")` against an isolated agent dir downloaded, verified, and installed a real, working `rg 14.1.1` binary end to end. `npx tsgo --noEmit` and `npx biome check` clean. |
| 12.6 | Settle Apex-only version authority and release artifact contract in ADRs. The owned release set is exactly `apex-code-agent-core` and `apex-code`; define exact dependency, tag, tarball, branding allowlist, provenance, and fake-provider smoke requirements. | — | complete | ADR 0018. Audited the actual release path first (`.github/workflows/release.yml`, `scripts/publish.mjs`, `scripts/apex/validate-release-tag.mjs`) and found it already correctly Apex-scoped and CI-enforced (exact dependency, matching versions, publish order, provenance). The gap was upstream of that: `scripts/release.mjs` and `scripts/sync-versions.js`, both inherited unmodified from Pi's monorepo tooling. |
| 12.7 | Replace inherited workspace-wide release/version behavior with Apex-only tooling and integration tests. Frozen consumed packages must remain untouched. | 12.6 | complete | Found and fixed real, previously-latent defects, verified against the actual repo tree before and after: `sync-versions.js`'s lockstep check required all 8 non-private workspace packages (2 Apex-owned + 6 frozen) to share one version and **failed outright** on the real tree (`0.0.1-alpha.1` vs `0.84.1`) — confirmed by running it. `release.mjs`'s `getVersion()` read `packages/ai` (a frozen package) as "the" current version; its bump path called `npm version --workspaces`, which would rewrite all 8 packages' `package.json` (including frozen ones, defeating `check-frozen-packages.mjs`'s byte-identity guarantee); `getChangelogs()` likewise targeted every package's `CHANGELOG.md`, not just the two owned ones. Also found `addUnreleasedSection`'s regex matched only `packages/agent`'s changelog heading ("# Changelog") and silently never matched `packages/coding-agent`'s ("# Apex Code changelog") — confirmed the literal regex behavior directly. Fixed: extracted `FROZEN_PACKAGE_DIRECTORIES` to a shared `scripts/apex/frozen-packages.mjs` (single source of truth for `check-frozen-packages.mjs` and `sync-versions.js`); `sync-versions.js` now keys lockstep validation and exact-dependency rewriting off `getPublicWorkspacePackages()` and never writes inside a frozen directory; `package.json`'s `version:patch/minor/major/set` scripts now pass `--workspace packages/agent --workspace packages/coding-agent` instead of `--workspaces`; `release.mjs`'s changelog logic was extracted to a testable `scripts/apex/release-changelogs.mjs` and its heading regex generalized. Verified: `node scripts/sync-versions.js` now passes cleanly against the real tree (previously exited 1); a real `npm version patch --workspace packages/agent --workspace packages/coding-agent` against an isolated fixture leaves a frozen-package stand-in untouched (`version-bump-scope.test.mjs`); `release-changelogs.test.mjs` proves the heading-regex fix against both real changelog headings; `sync-versions.test.mjs` rewritten with a frozen-package stand-in proving both the dependency-sync exclusion and the corrected lockstep scope. 39/39 `npm run test:scripts` tests pass; `npm run check` (biome, docs lifecycle, pinned-deps, ts-imports, scrubber typecheck, shrinkwrap, install-lock, `tsgo --noEmit`, browser-smoke) clean. |
| 12.8 | Add packed-artifact identity and functional gates. Pack exact tarballs, inspect metadata/README/dist/system prompt, run clean scratch installs and provider-independent sandbox/session smoke, reject secrets/absolute paths/unreviewed active Pi identity, and run before publication. | 12.6, 12.3, 12.4 | complete | `scripts/apex/packed-product-surface.mjs`: packs both Apex-owned packages for real (never `--dry-run`), extracts the tarball, and scans packed README + compiled `dist/*.js` for active Pi product-misidentification, secret-shaped strings, and build-machine absolute paths (deliberately narrower than a bare `/home/`/`/Users/` prefix, which also matches this codebase's own legitimate cross-platform path-handling literals — verified the narrower pattern doesn't false-positive on the real one found in `export-html/template.js`). Then installs both tarballs into an isolated scratch directory via `file:` dependencies (resolving the frozen packages from the real registry, the same shape a real end-user install takes) and runs a real provider-independent scripted turn through the real sandbox (`--smoke`). Live-verified three consecutive times against the actual current build (`npm run build` for both packages): identity scan clean, real npm install from the registry, real completed sandboxed turn every time. Investigated whether source maps (494 files, `inlineSources: true` embeds full original TS source) constitute a leak — confirmed zero secrets and zero build-machine absolute paths across all of `dist/`, so out of this gate's scope; left as a separate, undecided product-footprint question rather than an unreviewed fix. 7 tests in `packed-product-surface.test.mjs` (6 fast synthetic-fixture tests proving both detection and false-positive avoidance, always run; 1 real end-to-end pack+install+smoke test, opt-in via `APEX_PACKED_SMOKE_TEST=1` and skipped by default so ordinary `npm test` doesn't gain a real-network dependency — the release workflow enforces the equivalent check directly). Wired into `.github/workflows/release.yml` as a required step before either publish step, replacing the old `npm pack --dry-run` content-blind check; `release-workflow.test.mjs` gained a test asserting the gate step exists, passes `--smoke`, and runs before both `npm publish` steps. `npx tsgo --noEmit -p scripts/tsconfig.json` and full `npm run check` clean. |
| 12.9 | Add post-publication registry verification and release evidence. Compare tag SHA, registry `gitHead`, tarball manifest/hash, npm provenance, and clean installs on supported platforms; document immutable-version compromise/deprecation recovery. | 12.7, 12.8 | complete | `scripts/apex/verify-published-release.mjs`: fetches `npm view <name>@<version> --json`, compares the registry's `gitHead` against the release tag's commit SHA, re-downloads the actual tarball bytes and independently recomputes both sha1 (matching `dist.shasum`) and sha512 (matching `dist.integrity`), and confirms an npm provenance attestation (`dist.attestations`, SLSA v1 predicate) is attached. Live-verified against a real, already-published external package (`@earendil-works/pi-ai@0.84.1`, a frozen upstream dependency this repo already consumes — never one of Apex's own unpublished packages): gitHead, both hashes, and provenance all matched for real; a deliberately wrong expected `gitHead` was correctly flagged. 11 tests (8 fast fixture-based comparison-logic tests plus 3 real-network tests against that same published package) all pass in the default suite (~2.4s, no opt-in gating needed — much lighter than 12.8's full install). Wired into `.github/workflows/release.yml` as a required step after both `npm publish` steps, given the tag's own commit (`${GITHUB_SHA}`) as the expected `gitHead`; `release-workflow.test.mjs` gained a test asserting the step exists, checks both packages, passes the tag commit, and runs after both publishes. Wrote `docs/release-integrity-runbook.md`: concrete recovery command sequences (not just ADR 0014's ownership policy) for a compromised/bad published version (deprecate, investigate provenance, rotate, republish, re-verify), an accidental publish, and a stale `next` dist-tag — cross-referencing ADR 0014 for ownership and the still-open task 12.13 for the one item it explicitly does not cover (rotating GitHub's own OIDC trust relationship, an external-settings action, not a script). `npx tsgo --noEmit -p scripts/tsconfig.json` clean; full `node --test scripts/*.test.mjs scripts/apex/*.test.mjs` 59/60 passing (1 intentionally opt-in). |
| 12.10 | Add downloaded package/resource integrity policy for executable extensions and remote resources, if the implementation surface is covered by this phase; otherwise record a bounded follow-up rather than silently widening scope. | 12.2, 12.5 | complete — audited, no additional in-scope surface found | Audited every remote-fetch call site in `packages/coding-agent/src` for a second Apex-initiated download-and-execute path beyond `tools-manager.ts` (already covered by 12.5/ADR 0017). Found none: `src/extensions/llama/` lets a user point Apex at their own already-running llama.cpp server and asks *that server* to download model weights from Hugging Face — Apex Code itself never downloads, writes, or executes the weight file, so no integrity mechanism inside this repository applies (the user's own server is the trust boundary, unchanged by this phase). There is no remote extension/skill installation mechanism at all — extensions and skills load only from local filesystem paths (`--extension <path>`, project/user config directories); MCP server commands are user-configured local command execution (e.g. `npx <package>`), the same category `SECURITY.md` already declares out of scope ("the behavior of extensions or skills you chose to install"). `version-check.ts` and the self-update path fetch/install only through npm's own registry mechanism, covered by 12.9's provenance/hash verification, not a separate custom downloader. Per the task's own conditional wording, this is the bounded follow-up record, not scope creep: if a future phase adds a remote extension/skill install feature, it inherits ADR 0017's model (reviewed pin, bounded/verified/quarantined/atomic install), not a new policy. |
| 12.11 | Add supply-chain evidence: dependency vulnerability policy, scheduled scanning, SBOM, complete transitive production license closure, and release artifact hash/provenance attachments. | 12.7 | complete | **Scheduled scanning**: `.github/dependabot.yml`, scoped to root + the two Apex-owned packages only — deliberately excludes all six frozen package directories (a Dependabot PR editing one would immediately fail `check-frozen-packages.mjs`'s byte-identity check), verified by a test walking `FROZEN_PACKAGE_DIRECTORIES`. Also tracks `github-actions` for pinned-SHA updates. **Vulnerability policy**: `npm audit --omit=dev --audit-level=high` added as a required release-gate step before publication (verified clean against the real current tree: 0 vulnerabilities at every severity). **SBOM**: `scripts/apex/generate-sbom.mjs` wraps `npm sbom --sbom-format cyclonedx`, one document per Apex-owned package, with a real validity guard (npm sbom exits 0 even for a near-empty result; the guard requires more than just the scanned package's own self-entry) — live-verified: 124 components for `apex-code`, 95 for `apex-code-agent-core`, each carrying real dependency hashes. **Production license closure**: reused the packed-and-installed scratch install from 12.8 (`--node-modules` pointed at it) instead of the monorepo's own root `node_modules`, which also contains devDependencies no end user's install ever receives — verified this materially matters (123 production-only packages vs. 295 when including this repo's own dev tooling). **Release artifact hash/provenance attachments**: extended `verify-published-release.mjs` with `--manifest-out`, writing a durable JSON manifest (gitHead, shasum, integrity, tarball URL, provenance attestation URL, verified status) per package, uploaded as a `release-evidence` CI artifact alongside the SBOM and license report — live-verified end to end against `@earendil-works/pi-ai@0.84.1`. Wrote `docs/release-integrity-runbook.md` (already recorded under 12.9, covers compromise/deprecation recovery — the "release artifact... provenance" evidence this task also asked for). 6 new tests across `generate-sbom.test.mjs` and `dependabot-config.test.mjs`, plus a `release-workflow.test.mjs` test asserting the audit runs before publish and all three artifacts (sbom, release-evidence, third-party-licenses) upload. Full `node --test scripts/*.test.mjs scripts/apex/*.test.mjs`: 64/65 passing (1 intentionally opt-in); `npx tsgo --noEmit` (root and scripts) and `npm run check`-equivalent all clean. |
| 12.12 | Publish the sole-maintainer support contract in `SECURITY.md`, `docs/support.md`, README links, and release documentation. Include Frantz Chery as accountable maintainer, best-effort targets, latest-prerelease support line, platform/Node matrix, upstream cadence, provider regression detection, breaking-change policy, compromise runbook, and succession. | — | complete | New `docs/support.md`: names Frantz Chery, states best-effort framing explicitly, a target table (security acknowledgement/triage/fix, release authorization, provider-regression detection method, upstream merge cadence, breaking-change communication), the latest-non-deprecated-prerelease supported-version line, the Node/platform/sandbox-backend matrix (explicit about Windows sandbox exclusion under ADR 0005), a succession section, and a link to `docs/release-integrity-runbook.md` for compromise recovery. `SECURITY.md` gained a "Maintainer and support policy" section naming Frantz Chery and linking both docs, and a "Compromised or incorrect published release" section pointing at the runbook; its acknowledgement line now states the actual 5-business-day target instead of "a few days." The packed npm README (`packages/coding-agent/README.md`) gained a "Support and security" section with GitHub-absolute links (not relative — this file is packed into the npm tarball on its own, so a relative link to a monorepo-root doc would break for anyone reading it after `npm view` or on the npm registry page). 3 new tests in `support-policy.test.mjs` assert all of the above by content, not just file existence. Full `node --test scripts/*.test.mjs scripts/apex/*.test.mjs`: 67/68 passing (1 intentionally opt-in); `npx tsgo --noEmit`, docs-lifecycle validation, and full-repo `biome check` all clean. |
| 12.13 | Add governance workflow checks that are enforceable in-repository: pinned actions, OIDC/no token fallback, required artifact gates, frozen boundary, and release environment references. Record external GitHub settings as maintainer checklist items rather than claiming them enabled. | 12.8, 12.9, 12.12 | complete | Pinned-actions, OIDC-only (no `NPM_TOKEN`/`NODE_AUTH_TOKEN`), and required-artifact-gate ordering were already covered incrementally by tests added under 12.8/12.9/12.11; this task added the two still-missing in-repository checks and the external-settings record. New tests in `release-workflow.test.mjs`: the `publish` job references the `npm` GitHub Environment (`environment: npm`) — explicitly documented as provable only at the reference level, since whether that environment actually has required reviewers or a tag-restricted deployment policy is a GitHub setting no workflow file can assert; and the frozen-package-boundary check step exists and runs before both `Build` and the first `npm publish`. New `docs/release-governance-checklist.md`: an explicit, unchecked maintainer checklist (branch protection on `main`, the `npm` environment's deployment-branch/tag restriction and absence of a stored npm token, private vulnerability reporting, Dependabot alerts, npm Trusted Publishing binding scope, no leftover npm access tokens, 2FA) — worded throughout as "verify," never as "enabled," and tested for that framing (`support-policy.test.mjs`), not just for the items' presence. Cross-linked from `docs/support.md` and `docs/release-integrity-runbook.md`. Full `node --test scripts/*.test.mjs scripts/apex/*.test.mjs`: 70/71 passing (1 intentionally opt-in); `npx tsgo --noEmit`, docs-lifecycle validation, and full-repo `biome check` all clean. |
| 12.14 | Run required verification: focused suites, `npm run build`, `npm run check`, full `npm test`, documentation lifecycle validation, local packed install, then required Ubuntu/macOS/Windows CI from a spaced checkout. | 12.3–12.13 | complete | Local verification (see below) plus the required three-OS CI, both green. `npm run build` clean. `npm run check` clean. Full unscoped `npm test`: 0 failures locally (2811 tests). `node scripts/validate-docs-lifecycle.mjs` clean. Local packed install covered live, repeatedly, under 12.8/12.9/12.11. Required [CI run 31993235802](https://github.com/Fchery87/apex-code/actions/runs/31993235802) (commit `42ec17f93`) passed Ubuntu, macOS, Windows, and the frozen-package-boundary job from the asserted spaced checkout — but only after four real, previously-latent cross-platform bugs surfaced and were fixed through iterative required-CI runs (this is exactly what required three-OS CI is for; none of these were, or could have been, caught in this Linux-only dev environment): (1) a crash-cleanup fd leak in the Linux sandbox backend (fixed under 12.3, already covered above); (2) `npm`/`npm.cmd` cannot be invoked directly via `execFileSync`/`spawnSync` on Windows without `shell: true` (ENOENT then EINVAL) — added `scripts/apex/npm-command.mjs`'s `npmSpawnOptions()`, applied across `generate-sbom.mjs`, `packed-product-surface.mjs`, `verify-published-release.mjs`, and their tests; (3) `shell: true` does not itself quote array arguments containing spaces, silently truncating `--workspace` at the first space inside this repo's own required spaced checkout path — added `npmSpawnArgs()` (standard MS CRT command-line quoting), unit-tested directly against the exact path that broke; (4) the real-archive install test packed a binary named without the Windows `.exe` suffix the production code actually looks for there, and the accompanying macOS credential test asserted directory-level sibling-hiding that macOS's Seatbelt backend (a pure allow/deny rule layer, unlike Linux's mount-shadowing bwrap trick) does not straightforwardly provide and ADR 0015 does not require — fixed the fixture and narrowed the macOS test to what is both true and required (read works, write is rejected), documenting the sibling-hiding gap honestly rather than asserting it. |
| 12.15 | Publish a corrected prerelease only after all artifact gates pass, verify the downloaded registry package, deprecate stale alpha versions, and record immutable release evidence. Do not claim stable production support from this prerelease. | 12.9, 12.14 | not started | — |

## Implementation order

1. 12.1 and 12.2: security authority decisions.
2. 12.3 and 12.4: state and supervisor boundaries.
3. 12.5: executable artifact integrity.
4. 12.6: release/version and packed-artifact contract decisions.
5. 12.7–12.9: release tooling and artifact gates.
6. 12.10–12.13: resource integrity, supply-chain evidence, support policy, and enforceable governance checks.
7. 12.14: complete verification.
8. 12.15: corrected prerelease publication and deprecation of stale artifacts.

## Exit evidence

Phase 12 may be marked landed only when the specification's exit criterion is met with:

- Technical ADRs for credential/state handoff, trust-policy precedence, resource integrity,
  Apex version authority, and release artifact identity.
- Deterministic Linux/macOS handoff and supervisor-boundary tests.
- Tool/resource tamper-rejection tests.
- Apex-only release tooling integration tests.
- Pre- and post-publication artifact identity and functional smoke evidence.
- Required three-OS CI and frozen-package proof.
- Published sole-maintainer security/support policy and succession path.
- Registry evidence for the corrected prerelease, including exact version, `gitHead`, and
  tarball identity.

## Order changes

None. The security-boundary decisions are intentionally scheduled before implementation;
artifact gates depend on the resulting handoff and trust contracts.
